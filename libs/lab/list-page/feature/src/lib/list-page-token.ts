import { InjectionToken } from "@angular/core";
import { ListRowContext } from "@lab/list-page/infrastructure";

export const LIST_ROW_CONTEXT = new InjectionToken<ListRowContext>(
  "LIST_ROW_CONTEXT",
);
